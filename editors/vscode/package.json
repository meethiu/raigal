{
  "name": "raigal-vscode",
  "displayName": "Raigal",
  "description": "Surface Raigal code-quality findings as diagnostics in VS Code by shelling out to your installed Raigal CLI.",
  "version": "0.1.0",
  "publisher": "Raigal",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Linters"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "raigal.scanWorkspace",
        "title": "Raigal: Scan Workspace"
      }
    ],
    "configuration": {
      "title": "Raigal",
      "properties": {
        "raigal.path": {
          "type": "string",
          "default": "raigal",
          "scope": "application",
          "description": "User-level absolute path or PATH command for the Raigal CLI executable. Workspace settings are ignored for security."
        },
        "raigal.scanOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Re-scan a file when it is saved."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
